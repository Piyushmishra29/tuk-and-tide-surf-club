{
  "name": "tuk-and-tide-club",
  "version": "1.0.0",
  "description": "Tuk & Tide Surf Club — static website",
  "private": true,
  "scripts": {
    "build": "python3 build.py",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "puppeteer-core": "^25.2.1"
  }
}
